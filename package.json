{
  "name": "squissh",
  "version": "1.4.0",
  "private": true,
  "description": "SquiSSH — KDE Plasma widget for managing SSH connections",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
